{
  "_comment": "Drogon 테스트 서버 실행 설정 파일이다.",
  "_comment_listeners": "listeners는 로컬 HTTP 요청을 받을 주소/포트를 정의한다.",
  "listeners": [
    {
      "_comment": "루프백 주소에만 바인딩해 현재 PC에서만 접근 가능하게 한다.",
      "address": "127.0.0.1",
      "_comment_port": "기존 drogon-db-test와 동일하게 8080 포트를 사용한다.",
      "port": 8080
    }
  ],
  "_comment_db_clients": "db_clients는 Drogon이 관리할 이름 기반 DB 연결 목록이다.",
  "db_clients": [
    {
      "_comment_name": "코드에서 app().getDbClient(\"default\")로 이 연결을 가져온다.",
      "name": "default",
      "_comment_rdbms": "MySQL 백엔드를 사용한다.",
      "rdbms": "mysql",
      "_comment_host": "DB 서버 호스트 주소다.",
      "host": "127.0.0.1",
      "_comment_port": "MySQL 기본 포트(3306)다.",
      "port": 3306,
      "_comment_dbname": "기본 접속 스키마 이름이다.",
      "dbname": "test_db",
      "_comment_user": "DB 접속 계정이다.",
      "user": "drogon_test",
      "_comment_passwd": "DB 계정 비밀번호다.",
      "passwd": "drogon_test_pw!",
      "_comment_is_fast": "false면 일반 DbClient를 생성하고 getDbClient()로 사용한다.",
      "is_fast": false,
      "_comment_number_of_connections": "이 데모에서는 커넥션 풀 1개면 충분하다.",
      "number_of_connections": 1,
      "_comment_timeout": "DB 작업 타임아웃(초)이다.",
      "timeout": 5.0
    }
  ]
}
